name: targets_and_goals
description: Targets and Goals tracker.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.16.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  equatable: ^2.0.3
  uuid: ^3.0.6
  cupertino_icons: ^1.0.2
  flutter_bloc: ^8.0.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  dart_code_metrics: ^4.14.0

  flutter_lints: ^1.0.0

flutter:

  uses-material-design: true